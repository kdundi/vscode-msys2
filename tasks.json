{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build single file.",
            "type": "shell",
            "command": "c:\\msys64\\msys2_shell.cmd -mingw64 -c \"g++ '${file}' -o '${workspaceFolder}\\${fileBasenameNoExtension}.exe'\"",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}
